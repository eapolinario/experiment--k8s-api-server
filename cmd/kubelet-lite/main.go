// Package main is the kubelet-lite binary entrypoint.
//
// TODO(kubelet): list+watch Pods via client-go, reconcile against the local
// Docker daemon, patch status back to the apiserver.
package main

func main() {
	// Implemented in the `kubelet` phase.
}
